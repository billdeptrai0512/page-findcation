---
// Lightweight CSS-only (mostly) animation demo of Findcation app flow
import { ShieldCheck } from "lucide-react";
import fs from "node:fs";
import path from "node:path";

let selectedImages = [];
try {
  const staycationDir = path.join(process.cwd(), "public", "staycation");
  const files = fs.readdirSync(staycationDir).filter(file => file.match(/\.(jpg|jpeg|png|webp|avif)$/i));
  // Shuffle array
  files.sort(() => Math.random() - 0.5);
  selectedImages = files.slice(0, 9).map(f => `/staycation/${f}`);
} catch (e) {
  console.error("Error reading staycation directory:", e);
  selectedImages = [
    "/staycation/12.jpg", 
    "/staycation/476583676_1677470439646021_1849087174596840875_n.jpg", 
    "/staycation/481454916_122100282974782033_72008881661737480_n.jpg"
  ];
}

// Fallback if directory is empty
if (selectedImages.length === 0) {
  selectedImages = [
    "https://picsum.photos/400/250?random=10",
    "https://picsum.photos/400/250?random=11",
    "https://picsum.photos/400/250?random=12"
  ];
}
---

<div class="demo-container">
  <div class="phone-frame">
    <div class="phone-notch"></div>
    <div class="phone-screen" id="demo-screen" data-step="0">
      
      <!-- Map Background -->
      <div class="map-layer">
        <img class="map-bg-image" src="/island_demo.png" alt="Vietnam Map" />
      </div>
      
      <!-- Top Left Info Card -->
      <div class="demo-info-card">
        <div class="demo-info-title">Findcation</div>
        <div class="demo-info-desc">Bản đồ chỗ ở checkin tự động<br> khắp Việt Nam</div>
      </div>
      
      <!-- Island Labels -->
      <div class="demo-island-label hoang-sa">quần đảo Hoàng Sa</div>
      <div class="demo-island-label truong-sa">quần đảo Trường Sa</div>
      
      <!-- Map UI -->
      <button class="nearby-btn">Gần tôi</button>
      <button class="mystay-btn">Staycation của tôi</button>
      
      <!-- User Location -->
      <div class="user-marker">
        <div class="user-pulse"></div>
        <img src="/people.webp" alt="User Location" class="user-core" />
      </div>
      
      <!-- Group 1 Markers -->
      <div class="stay-marker sm-1 group-1">
        <img src="/home.webp" alt="Staycation" />
      </div>
      <div class="stay-marker sm-2 group-1">
        <img src="/home.webp" alt="Staycation" />
      </div>
      <div class="stay-marker sm-3 group-1 target-marker">
        <img src="/home.webp" alt="Staycation" />
      </div>
      
      <!-- Group 2 Markers -->
      <div class="stay-marker sm-4 group-2">
        <img src="/home.webp" alt="Staycation" />
      </div>
      <div class="stay-marker sm-5 group-2">
        <img src="/home.webp" alt="Staycation" />
      </div>
      <div class="stay-marker sm-6 group-2">
        <img src="/home.webp" alt="Staycation" />
      </div>
      <div class="stay-marker sm-7 group-2">
        <img src="/home.webp" alt="Staycation" />
      </div>

      <!-- ===================== FULL-SCREEN LISTING PANEL ===================== -->
      <div class="listing-panel">
        
        <!-- Sticky Header -->
        <div class="card-header">
          <button class="back-btn">←</button>
          <img src="/logo.webp" alt="Findcation" class="brand-logo-img" />
        </div>

        <!-- Scrollable Panel Content -->
        <div class="panel-scroll-area">

          <!-- Image Slider -->
          <div class="img-slider-wrap">
            <div class="img-slider">
              {selectedImages.map((imgSrc, index) => (
                <div class="img-slide">
                  <img src={imgSrc} alt={`Room ${index + 1}`} />
                </div>
              ))}
            </div>
            <div class="slider-dots">
              {selectedImages.map((_, index) => (
                <span class={`dot ${index === 0 ? 'active' : ''}`}></span>
              ))}
            </div>
          </div>


          <!-- Detail Cards -->
          <div class="detail-cards">

            <div class="detail-card">
              <div class="card-row-header">
                <span class="card-icon">✦</span>
                <span class="card-label">Tiêu đề</span>
              </div>
              <p class="card-content">Villa Quận 1</p>
            </div>

            <div class="detail-card">
              <div class="card-row-header">
                <span class="card-icon">🏠</span>
                <span class="card-label">Cho thuê</span>
              </div>
              <p class="card-content">Toàn bộ căn nhà</p>
            </div>

            <div class="detail-card">
              <div class="card-row-header">
                <span class="card-icon">💰</span>
                <span class="card-label">Chi phí</span>
              </div>
              <p class="card-content price-highlight">300.000đ <span class="price-arrow">→</span> 750.000đ</p>
            </div>

            <div class="detail-card">
              <div class="card-row-header">
                <span class="card-icon">📍</span>
                <span class="card-label">Địa chỉ</span>
              </div>
              <p class="card-content">12 Nguyễn Huệ - Phường Bến Nghé - TP. Hồ Chí Minh</p>
            </div>

            <!-- Features card (collapsible) -->
            <div class="detail-card features-card">
              <div class="card-row-header features-toggle">
                <span class="card-icon">✦</span>
                <span class="card-label">Tiện nghi</span>
                <span class="features-count">6</span>
                <span class="chevron">˅</span>
              </div>
              <div class="features-list collapsed">
                <div class="feature-row"><span>📶 Wifi</span></div>
                <div class="feature-row"><span>❄️ Điều hòa</span></div>
                <div class="feature-row"><span>🅿️ Bãi đỗ xe</span></div>
                <div class="feature-row"><span>🫧 Máy giặt</span></div>
                <div class="feature-row"><span>🍳 Bếp</span></div>
                <div class="feature-row last"><span>🌿 Ban công</span></div>
              </div>
            </div>
            
            <div class="contact-panel">
            <div class="contact-header">Thông tin liên hệ</div>
            <div class="contact-body">
              <div class="contact-icons">
                <div class="contact-icon-wrap">
                  <div class="ci-icon-box">
                    <img src="/facebook.webp" alt="Facebook" class="ci-img" />
                    <span class="shield-badge verified">
                      <ShieldCheck size={14} strokeWidth={2.5} />
                    </span>
                  </div>
                  <span class="ci-label">Facebook</span>
                </div>
                <div class="contact-icon-wrap">
                  <div class="ci-icon-box">
                    <img src="/instagram.webp" alt="Instagram" class="ci-img" />
                    <span class="shield-badge verified">
                      <ShieldCheck size={14} strokeWidth={2.5} />
                    </span>
                  </div>
                  <span class="ci-label">Instagram</span>
                </div>
                <div class="contact-icon-wrap">
                  <div class="ci-icon-box zalo-icon-box">
                    <img src="/zalo.webp" alt="Zalo" class="ci-img" />
                    <span class="shield-badge verified">
                      <ShieldCheck size={14} strokeWidth={2.5} />
                    </span>
                  </div>
                  <span class="ci-label">Zalo</span>
                </div>
              </div>
            </div>
            
        </div>

          </div><!-- /detail-cards -->

          <!-- Spacer so footer doesn't overlap content -->
          <div style="height: 80px;"></div>
        </div><!-- /panel-scroll-area -->

      </div><!-- /listing-panel -->

      <!-- App Logo Screen (Overlay) -->
      <div class="app-logo-screen">
        <img src="/logo.webp" alt="Findcation Logo" class="app-logo" />
      </div>

      <!-- Tap Pointer Interaction -->
      <div class="pointer-cursor"></div>
      
    </div>
  </div>
</div>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

  :root {
    --primary: #E61E4D;
    --radius-sm: 6px;
    --radius-md: 10px;
    --shadow-sm: 0 2px 6px rgba(0,0,0,0.07);
    --shadow-md: 0 4px 14px rgba(0,0,0,0.11);
  }

  .demo-container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 1rem;
    width: 100%;
    font-family: 'Inter', sans-serif;
  }

  .phone-frame {
    width: 280px;
    height: 560px;
    background: white;
    border-radius: 40px;
    box-shadow: 
      0 0 0 10px #111827,
      0 20px 40px -10px rgba(0, 0, 0, 0.2),
      0 0 0 11px rgba(255,255,255,0.2) inset;
    position: relative;
    overflow: hidden;
  }

  .phone-notch {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 24px;
    background: #111827;
    border-bottom-left-radius: 16px;
    border-bottom-right-radius: 16px;
    z-index: 200;
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    position: relative;
    background: #f3f4f6;
    overflow: hidden;
  }

  /* ---- App Logo Screen ---- */
  .app-logo-screen {
    position: absolute;
    inset: 0;
    background: white;
    z-index: 90;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: opacity 0.6s ease, visibility 0.6s ease;
    opacity: 0;
    visibility: hidden;
  }
  .app-logo {
    width: 180px;
    height: auto;
    object-fit: contain;
  }

  /* ---- Map Background ---- */
  .map-layer {
    position: absolute;
    inset: -200px;
    background-color: #f4f6f8;
    transform-origin: center;
    transition: transform 1.2s cubic-bezier(0.2, 0.8, 0.2, 1);
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .map-bg-image {
    width: 70%;
    height: 80%;
    object-fit: cover;
    object-position: center;
    pointer-events: none;
    opacity: 0.95;
    transform: translateX(8px);
    mix-blend-mode: multiply;
  }

  /* ---- Map Info Card & Island Labels ---- */
  .demo-info-card {
    position: absolute;
    top: 36px;
    left: 16px;
    right: 32px;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(8px);
    border-radius: 12px;
    padding: 12px 14px;
    box-shadow: 0 4px 16px rgba(0,0,0,0.1);
    z-index: 45;
    border: 1px solid rgba(255,255,255,0.8);
  }
  .demo-info-title {
    font-size: 16px;
    font-weight: 700;
    color: #111827;
    margin-bottom: 4px;
  }
  .demo-info-desc {
    font-size: 11px;
    color: #4b5563;
    line-height: 1.4;
  }

  .demo-island-label {
    position: absolute;
    font-size: 10px;
    color: #6b7280;
    font-weight: 500;
    background: rgba(255,255,255,0.7);
    padding: 2px 6px;
    border-radius: 4px;
    z-index: 40;
    pointer-events: none;
  }
  .hoang-sa {
    top: 40%;
    right: 15%;
  }
  .truong-sa {
    bottom: 25%;
    right: 8%;
  }

  /* ---- Map Buttons ---- */
  .nearby-btn {
    position: absolute;
    bottom: 32px;
    left: 16px;
    background: #3b82f6;
    border: none;
    border-radius: 20px;
    padding: 10px 16px;
    font-size: 13px;
    font-weight: 600;
    color: white;
    box-shadow: 0 4px 12px rgba(59,130,246,0.3);
    z-index: 50;
    transition: transform 0.2s, background-color 0.2s;
    font-family: 'Inter', sans-serif;
  }
  .mystay-btn {
    position: absolute;
    bottom: 32px;
    right: 16px;
    background: var(--primary);
    border: none;
    border-radius: 20px;
    padding: 10px 16px;
    font-size: 13px;
    font-weight: 600;
    color: white;
    box-shadow: 0 4px 12px rgba(230,30,77,0.3);
    z-index: 50;
    transition: transform 0.2s, background-color 0.2s;
    font-family: 'Inter', sans-serif;
  }

  /* ---- User Marker ---- */
  .user-marker {
    position: absolute;
    top: 50%;
    left: 45%;
    transform: translate(-50%, -50%) scale(0);
    z-index: 40;
    transition: transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
    opacity: 0;
  }
  .user-core {
    width: 48px;
    height: 48px;
    position: relative;
    z-index: 2;
    object-fit: contain;
    filter: drop-shadow(0 2px 6px rgba(0,0,0,0.2));
  }
  .user-pulse {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 64px;
    height: 64px;
    background: rgba(59, 130, 246, 0.2);
    border-radius: 50%;
    animation: pulse 2s infinite cubic-bezier(0.4, 0, 0.6, 1);
    z-index: 1;
  }
  @keyframes pulse {
    0% { transform: translate(-50%, -50%) scale(0.3); opacity: 1; }
    100% { transform: translate(-50%, -50%) scale(1); opacity: 0; }
  }

  /* ---- Staycation Markers ---- */
  .stay-marker {
    position: absolute;
    width: 44px;
    height: 44px;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 30;
    transform: translate(-50%, -50%) scale(0);
    opacity: 0;
    transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1), filter 0.3s;
  }
  .stay-marker img { 
    width: 100%; 
    height: 100%; 
    object-fit: contain;
    filter: drop-shadow(0 4px 12px rgba(227, 28, 95, 0.2));
  }

  .sm-1 { top: 38%; left: 25%; transition-delay: 0.1s; }
  .sm-2 { top: 65%; left: 35%; transition-delay: 0.2s; }
  .sm-3 { top: 40%; left: 75%; transition-delay: 0.0s; z-index: 35; }
  .sm-4 { top: 20%; left: 65%; transition-delay: 0.1s; }
  .sm-5 { top: 80%; left: 70%; transition-delay: 0.2s; }
  .sm-6 { top: 75%; left: 15%; transition-delay: 0.3s; }
  .sm-7 { top: 15%; left: 20%; transition-delay: 0.4s; }

  /* ---- Pointer Cursor ---- */
  .pointer-cursor {
    position: absolute;
    width: 32px;
    height: 32px;
    background-image: url('data:image/svg+xml;utf8,<svg width="32" height="32" viewBox="0 0 24 24" fill="%23ffffff" xmlns="http://www.w3.org/2000/svg"><path d="M12,2A4,4 0 0 1 16,6V13L16.2,13.2C16.6,13.6 17.5,14 18,14C19.1,14 20,14.9 20,16C20,16.5 19.8,17 19.4,17.4L13.4,23.4C13,23.8 12.5,24 12,24H7C5.9,24 5,23.1 5,22V11L5.7,11C6.1,11 6.5,10.8 6.8,10.6L10,8V6A4,4 0 0 1 12,2Z" stroke="%23333333" stroke-width="1.5"/></svg>');
    background-repeat: no-repeat;
    z-index: 999;
    pointer-events: none;
    top: 50%;
    left: 0%;
    opacity: 0;
    transform-origin: top left;
    transform: translateZ(10px);
    transition: top 0.6s ease-in-out, left 0.6s ease-in-out, transform 0.2s ease-out, opacity 0.3s ease;
  }

  /* ====================== LISTING PANEL ====================== */
  .listing-panel {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 100%;
    background: #fafbfc;
    border-top-left-radius: 18px;
    border-top-right-radius: 18px;
    box-shadow: 0 -4px 24px rgba(0,0,0,0.12);
    z-index: 60;
    transform: translateY(110%);
    transition: transform 0.6s cubic-bezier(0.2, 0.8, 0.2, 1);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    font-family: 'Inter', sans-serif;
  }

  /* Sticky Header */
  .card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 12px 10px;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid #f0f0f0;
    flex-shrink: 0;
    z-index: 10;
  }
  .back-btn {
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    padding: 5px 9px;
    background: white;
    font-size: 14px;
    font-weight: 600;
    color: #333;
    cursor: pointer;
    line-height: 1;
    font-family: 'Inter', sans-serif;
  }
  .brand-logo-img {
    height: 28px;
    width: auto;
    object-fit: contain;
  }

  /* Scrollable area - this is what the pointer "scrolls" */
  .panel-scroll-area {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    scrollbar-width: none;
    transform: translateY(0);
    transition: transform 0.8s ease;
  }
  .panel-scroll-area::-webkit-scrollbar { display: none; }

  /* Image Slider */
  .img-slider-wrap {
    position: relative;
    background: #1c1e21;
  }
  .img-slider {
    display: flex;
    overflow-x: scroll;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
  }
  .img-slider::-webkit-scrollbar { display: none; }
  .img-slide {
    flex: 0 0 100%;
    scroll-snap-align: start;
  }
  .img-slide img {
    width: 100%;
    height: 160px;
    object-fit: cover;
    display: block;
  }
  .slider-dots {
    position: absolute;
    bottom: 8px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 4px;
    background: rgba(0,0,0,0.35);
    padding: 3px 7px;
    border-radius: 10px;
  }
  .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #bbb;
  }
  .dot.active { background: #fff; }

  /* Section padding */
  .section-pad {
    padding: 10px 12px 4px;
  }

  /* Verified Badge */
  .verified-badge {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 5px 10px;
    background: linear-gradient(135deg, #e8f5e9, #c8e6c9);
    border: 1px solid #81c784;
    border-radius: 16px;
    font-size: 0.75rem;
    font-weight: 500;
    color: #2e7d32;
    box-shadow: 0 2px 6px rgba(76,175,80,0.15);
  }

  /* Detail Cards */
  .detail-cards {
    display: flex;
    flex-direction: column;
    gap: 8px;
    padding: 8px 12px;
  }
  .detail-card {
    background: white;
    border-radius: var(--radius-md);
    padding: 10px 12px;
    box-shadow: var(--shadow-sm);
    border: 1px solid #f0f0f0;
    transition: all 0.3s;
  }
  .card-row-header {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 5px;
  }
  .card-icon {
    font-size: 12px;
    color: #E61E4D;
  }
  .card-label {
    font-size: 0.8rem;
    font-weight: 600;
    color: #222;
    flex: 1;
  }
  .card-content {
    font-size: 0.82rem;
    color: #555;
    line-height: 1.5;
    margin: 0;
    padding-left: 18px;
  }
  .price-arrow { color: #999; margin: 0 2px; }
  .price-highlight { color: #E61E4D; font-weight: 600; }

  /* Features card */
  .features-toggle {
    cursor: pointer;
  }
  .features-count {
    font-size: 0.75rem;
    font-weight: 600;
    color: #E61E4D;
    background: #fff1f4;
    border-radius: 10px;
    padding: 1px 6px;
    margin-left: 2px;
  }
  .chevron {
    font-size: 13px;
    color: #999;
    transition: transform 0.3s;
  }
  .features-list {
    margin-top: 6px;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease;
  }
  .features-list.open {
    max-height: 200px;
  }
  .features-list.collapsed {
    max-height: 0;
  }
  .feature-row {
    display: flex;
    justify-content: space-between;
    padding: 7px 0;
    border-bottom: 1px solid #f3f3f3;
    font-size: 0.8rem;
    color: #555;
    padding-left: 18px;
  }
  .feature-row.last { border-bottom: none; }

  /* ---- Panel Footer ---- */
  .panel-footer {
    flex-shrink: 0;
    background: white;
    border-top: 1px solid #f0f0f0;
    padding: 10px 12px;
  }

  .footer-default {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .btn-primary {
    background: linear-gradient(to right, #E61E4D, #E31C5F, #D70466);
    color: white;
    padding: 11px;
    border-radius: var(--radius-md);
    font-weight: 600;
    font-size: 13px;
    border: none;
    cursor: pointer;
    width: 100%;
    transition: all 0.2s;
    font-family: 'Inter', sans-serif;
  }
  .btn-secondary {
    border: 1px solid #e0e0e0;
    background: white;
    padding: 8px;
    border-radius: var(--radius-sm);
    font-weight: 500;
    font-size: 12px;
    cursor: pointer;
    width: 100%;
    color: #555;
    font-family: 'Inter', sans-serif;
    transition: all 0.2s;
  }

  /* Contact Panel (always visible by default) */
  .contact-panel {
    display: flex;
    flex-direction: column;
    background: white;
    border-radius: var(--radius-md);
    overflow: hidden;
    border: 1px solid #e0e0e0;
    box-shadow: var(--shadow-md);
  }
  .contact-header {
    background: linear-gradient(135deg, #f0f7ff, #e8f4f8);
    border-bottom: 1px solid #d0e8f2;
    padding: 8px 12px;
    font-size: 0.8rem;
    font-weight: 600;
    color: #1976d2;
    text-align: center;
  }
  .contact-body {
    padding: 10px 8px 8px;
  }
  .contact-icons {
    display: flex;
    justify-content: center;
    gap: 16px;
  }
  .contact-icon-wrap {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 5px;
    position: relative;
  }
  .ci-facebook, .ci-instagram, .ci-zalo {
    width: 44px;
    height: 44px;
    border-radius: 13px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 18px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    border: 2px solid #f0f0f0;
    transition: transform 0.2s;
  }
  .ci-facebook {
    background: #1877F2;
    color: white;
    font-family: Georgia, serif;
  }
  .ci-instagram {
    background: linear-gradient(45deg, #f09433, #dc2743, #bc1888);
    color: white;
    font-size: 16px;
  }
  .ci-zalo {
    background: #0068FF;
    color: white;
    font-weight: 900;
    font-size: 16px;
  }
  .ci-label {
    font-size: 0.7rem;
    font-weight: 500;
    color: #444;
  }

  /* Icon box with webp image + shield badge */
  .ci-icon-box {
    position: relative;
    width: 48px;
    height: 48px;
    border-radius: 16px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.12);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .ci-img {
    width: 32px;
    height: 32px;
    object-fit: contain;
  }

  .shield-badge {
    position: absolute;
    top: -6px;
    right: -6px;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: white;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 1px 4px rgba(0,0,0,0.15);
  }
  .shield-badge.verified {
    color: #4caf50;
  }

  /* ====================== ANIMATION DATA-STEP STATES ====================== */

  /* Step 0: Logo visible */
  [data-step="0"] .app-logo-screen {
    opacity: 1;
    visibility: visible;
  }
  [data-step="0"] .pointer-cursor { opacity: 0; }
  [data-step="1"] .pointer-cursor { opacity: 0; }

  /* Step 2: Pointer moves to Gần tôi button */
  [data-step="2"] .pointer-cursor {
    opacity: 1;
    top: calc(100% - 50px);
    left: 60px;
  }

  /* Step 3: Click Gần tôi button */
  [data-step="3"] .pointer-cursor {
    opacity: 1;
    top: calc(100% - 50px);
    left: 60px;
    transform: scale(0.85);
  }
  [data-step="3"] .nearby-btn {
    transform: scale(0.92);
    background-color: #2563eb;
  }

  /* Step 4: User marker + group-1 pop */
  [data-step="4"] .user-marker,
  [data-step="5"] .user-marker,
  [data-step="6"] .user-marker,
  [data-step="7"] .user-marker,
  [data-step="8"] .user-marker,
  [data-step="9"] .user-marker,
  [data-step="10"] .user-marker {
    transform: translate(-50%, -50%) scale(1);
    opacity: 1;
  }
  [data-step="4"] .pointer-cursor { opacity: 0; top: 75%; left: 90%; }
  [data-step="4"] .nearby-btn,
  [data-step="5"] .nearby-btn,
  [data-step="6"] .nearby-btn,
  [data-step="7"] .nearby-btn,
  [data-step="8"] .nearby-btn,
  [data-step="9"] .nearby-btn,
  [data-step="10"] .nearby-btn {
    background-color: #EFF6FF;
    color: #2563EB;
  }
  [data-step="4"] .group-1,
  [data-step="5"] .group-1,
  [data-step="6"] .group-1,
  [data-step="7"] .group-1,
  [data-step="8"] .group-1,
  [data-step="9"] .group-1,
  [data-step="10"] .group-1 {
    transform: translate(-50%, -50%) scale(1);
    opacity: 1;
  }

  /* Step 5: Zoom out + group-2 */
  [data-step="5"] .map-layer,
  [data-step="6"] .map-layer,
  [data-step="7"] .map-layer,
  [data-step="8"] .map-layer,
  [data-step="9"] .map-layer,
  [data-step="10"] .map-layer { transform: scale(0.8); }
  [data-step="5"] .group-2,
  [data-step="6"] .group-2,
  [data-step="7"] .group-2,
  [data-step="8"] .group-2,
  [data-step="9"] .group-2,
  [data-step="10"] .group-2 {
    transform: translate(-50%, -50%) scale(1);
    opacity: 1;
  }
  [data-step="5"] .pointer-cursor { opacity: 0; top: 75%; left: 90%; }

  /* Step 6: Pointer moves to target marker */
  [data-step="6"] .pointer-cursor {
    opacity: 1;
    top: 42%;
    left: 78%;
  }

  /* Step 7: Click target marker */
  [data-step="7"] .pointer-cursor {
    opacity: 1;
    top: 42%;
    left: 78%;
    transform: scale(0.85);
  }
  [data-step="7"] .target-marker,
  [data-step="8"] .target-marker,
  [data-step="9"] .target-marker,
  [data-step="10"] .target-marker {
    transform: translate(-50%, -50%) scale(1.15);
    z-index: 45;
  }
  [data-step="7"] .target-marker img,
  [data-step="8"] .target-marker img,
  [data-step="9"] .target-marker img,
  [data-step="10"] .target-marker img {
    filter: drop-shadow(0 0 8px rgba(227, 28, 95, 0.6));
  }

  /* Step 8: Panel slides up, pointer at bottom ready to scroll */
  [data-step="8"] .listing-panel,
  [data-step="9"] .listing-panel,
  [data-step="10"] .listing-panel {
    transform: translateY(0);
  }
  [data-step="8"] .pointer-cursor {
    opacity: 1;
    top: 80%;
    left: 50%;
    z-index: 150;
  }

  /* Step 9: Pointer swipes UP (scroll sim) — content scrolls, footer revealed */
  [data-step="9"] .pointer-cursor {
    opacity: 1;
    top: 30%;
    left: 50%;
    z-index: 150;
  }
  /* Step 9 scroll is handled via JS scrollTop, not CSS transform */

  /* Step 10: Pointer moves to contact icon (Zalo) */
  [data-step="10"] .pointer-cursor,
  [data-step="11"] .pointer-cursor {
    opacity: 1;
    top: calc(100% - 50px);
    left: calc(50% + 64px);
    z-index: 999;
  }

  /* Step 11: Pointer clicks contact icon */
  [data-step="11"] .pointer-cursor {
    transform: scale(0.8) translateZ(10px);
  }
  [data-step="11"] .zalo-icon-box {
    transform: scale(0.88);
  }
  [data-step="11"] .zalo-icon-box::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    border-radius: 16px;
    background: rgba(0, 104, 255, 0.4);
    pointer-events: none;
    animation: demoClickRipple 0.6s ease-out forwards;
  }

  @keyframes demoClickRipple {
    0% { transform: scale(1); opacity: 0.8; }
    100% { transform: scale(1.6); opacity: 0; }
  }
</style>

<script>
  function initDemoController() {
    const screen = document.getElementById('demo-screen');
    if (!screen) return;
    
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
    if (prefersReducedMotion.matches) {
      screen.setAttribute('data-step', '10');
      return;
    }

    // Timeline
    const timeline = [
      { step: 0, delay: 1400 },  // Logo screen
      { step: 1, delay: 600 },   // Logo fades, map visible
      { step: 2, delay: 800 },   // Pointer moves to Gần tôi
      { step: 3, delay: 400 },   // Click Gần tôi
      { step: 4, delay: 1000 },  // User marker + group-1 markers
      { step: 5, delay: 900 },   // Zoom out + group-2
      { step: 6, delay: 800 },   // Pointer moves to marker
      { step: 7, delay: 400 },   // Click marker
      { step: 8, delay: 1000 },  // Full panel slides up
      { step: 9, delay: 1100 },  // Pointer swipes up (scroll)
      { step: 10, delay: 1200 }, // Pointer moves to Zalo icon and pauses
      { step: 11, delay: 2500 }  // Click Zalo icon & show ripple
    ];

    let currentIndex = 0;
    let timeoutId;

    // Custom scroll animation matching the pointer's CSS transition (0.6s ease-in-out)
    function smoothScrollTo(el, targetTop, duration) {
      const startTop = el.scrollTop;
      const distance = targetTop - startTop;
      const startTime = performance.now();
      function easeInOut(t) {
        return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;
      }
      function step(now) {
        const elapsed = now - startTime;
        const progress = Math.min(elapsed / duration, 1);
        el.scrollTop = startTop + distance * easeInOut(progress);
        if (progress < 1) requestAnimationFrame(step);
      }
      requestAnimationFrame(step);
    }

    function runAnimation() {
      if (!document.getElementById('demo-screen')) return;
      
      const currentFrame = timeline[currentIndex];
      const stepNum = currentFrame.step;
      screen.setAttribute('data-step', stepNum.toString());
      
      // Scroll panel content via scrollTop (avoids overflow clipping from CSS transform)
      const scrollArea = screen.querySelector('.panel-scroll-area');
      if (scrollArea) {
        if (stepNum >= 9) {
          smoothScrollTo(scrollArea, 160, 600); // 600ms  = pointer CSS transition duration
        } else if (stepNum < 8) {
          scrollArea.scrollTop = 0;
        }
      }
      
      document.dispatchEvent(new CustomEvent('demo-step-change', { 
        detail: { step: stepNum } 
      }));
      
      if (currentIndex === timeline.length - 1) {
        setTimeout(() => {
          if (document.getElementById('demo-screen')) {
            screen.setAttribute('data-step', '0');
            const sa = screen.querySelector('.panel-scroll-area');
            if (sa) sa.scrollTop = 0;
            
            // Randomize images when motion restarts
            const sliderImages = screen.querySelectorAll('.img-slide img');
            if (sliderImages.length > 0) {
              const srcs = Array.from(sliderImages).map(img => img.src);
              // Shuffle array using Fisher-Yates approach
              for (let i = srcs.length - 1; i > 0; i--) {
                const j = Math.floor(Math.random() * (i + 1));
                [srcs[i], srcs[j]] = [srcs[j], srcs[i]];
              }
              // Re-assign shufffled sources back
              sliderImages.forEach((img, i) => {
                img.src = srcs[i];
              });
            }
          }
        }, currentFrame.delay - 400);
      }
      
      currentIndex = (currentIndex + 1) % timeline.length;
      timeoutId = setTimeout(runAnimation, currentFrame.delay);
    }

    // Features toggle
    const featCard = screen.querySelector('.features-card');
    if (featCard) {
      featCard.querySelector('.features-toggle')?.addEventListener('click', () => {
        const list = featCard.querySelector('.features-list');
        const chevron = featCard.querySelector('.chevron');
        if (list) {
          list.classList.toggle('collapsed');
          list.classList.toggle('open');
          if (chevron) chevron.style.transform = list.classList.contains('open') ? 'rotate(180deg)' : '';
        }
      });
    }

    timeoutId = setTimeout(runAnimation, 700);

    document.addEventListener('astro:before-swap', () => {
      clearTimeout(timeoutId);
    }, { once: true });
  }

  document.addEventListener('DOMContentLoaded', initDemoController);
  document.addEventListener('astro:page-load', initDemoController);
</script>
